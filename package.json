{
  "name": "studio-fit-notebook",
  "version": "0.0.0",
  "private": true,
  "description": "Studio Fit Notebook — monorepo root.",
  "packageManager": "pnpm@10.23.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "tokens:check": "pnpm --filter @studio-fit/design-tokens exec tsc --noEmit",
    "tokens:build": "node packages/design-tokens/scripts/generate-tailwind-theme.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
